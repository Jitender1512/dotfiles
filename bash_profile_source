# To use this config file copy it to $HOME as .bash_profile_source and
# add following line at the end to $HOME/.bash_profile:
# source "$HOME/.bash_profile_source"

[[ -s "$HOME/.profile" ]] && source "$HOME/.profile" # Load the default .profile

# Paths
SMLNJ_HOME="/usr/share/smlnj"
PATH=$PATH:/usr/share/smlnj/bin/
PATH=$PATH:/usr/racket/
PATH=$PATH:/usr/racket/bin
PATH=$PATH:$HOME/.rvm/bin # Add RVM to PATH for scripting
PATH=$HOME/local/bin:$PATH # Allow local bin to override system
PATH=$PATH:$HOME/node-latest/bin
PATH=$PATH:$HOME/bin

[[ -s "$HOME/.rvm/scripts/rvm" ]] && source "$HOME/.rvm/scripts/rvm" # Load RVM into a shell session *as a function*
